import styles from './ProblemBand.module.css';

export default function ProblemBand() {
  return (
    <div className={styles.band}>
      <p>
        Owner organizations are managing billion-dollar pipeline projects with paper tickets,
        spreadsheets, and no independent field record — on any contract type.
      </p>
    </div>
  );
}
