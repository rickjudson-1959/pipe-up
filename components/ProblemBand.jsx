import styles from './ProblemBand.module.css';

export default function ProblemBand() {
  return (
    <div className={styles.band}>
      <p>
        On any contract — T&amp;M, Target Priced, or Lump Sum — owner reps lack an
        independent field record to verify what the contractor actually did.
      </p>
    </div>
  );
}
